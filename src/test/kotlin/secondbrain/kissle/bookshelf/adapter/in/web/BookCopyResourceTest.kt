package secondbrain.kissle.bookshelf.adapter.`in`.web

class BookCopyResourceTest {

    fun setup() {
        TODO("Implelment tests for BookCopyResource. Currently its failing because it tries to connect to the real database instead the testcontainer.")
    }
}