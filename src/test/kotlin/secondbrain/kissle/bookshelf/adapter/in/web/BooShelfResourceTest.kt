package secondbrain.kissle.bookshelf.adapter.`in`.web

class BooShelfResourceTest {

    fun setup() {
        TODO("Implelment tests for BooShelfResource. Currently its failing because it tries to connect to the real database instead the testcontainer.")
    }
}