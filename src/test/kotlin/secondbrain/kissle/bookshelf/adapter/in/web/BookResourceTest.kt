package secondbrain.kissle.bookshelf.adapter.`in`.web

class BookResourceTest {

    fun setup() {
        TODO("Implelment tests for BookResource. Currently its failing because it tries to connect to the real database instead the testcontainer.")
    }
}